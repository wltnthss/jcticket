<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org.//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jcticket.viewdetail.viewdetailMapper">

<!--    공연회차정보,시퀀스 조회-->
    <select id="select_showing_info" resultType="map">
        select showing_info,showing_seq
        from showing
        where showing_date = #{dateText}
    </select>


<!--    좌석가격 조회-->
    <select id="seat_price" resultType="int">
        select seat_price
        from seat_class
    </select>


<!--    잔여석 조회-->
    <select id="remain_seat" resultType="int">
        select
            s.showing_seat_cnt -
            (select count(*)
            from show_seat ss
            where s.showing_seq = ss.showing_seq
            and show_seat_status = 'N') as remaining_seats
        from
            showing s
        where s.showing_seq = #{showing_seq};
    </select>

<!--    공연날짜 조회-->
    <select id="showing_date" resultType="String">
        select showing_date
        from showing
        order by showing_date asc;
    </select>
</mapper>